blocks = int(input("Enter number of blocks: "))

level = 0
needed = 1
used = 0

while blocks >= needed:
    blocks -= needed
    used += needed
    level += 1
    needed += 1

print(f"Pyramid has {level} levels ")
print(f"Pyramid has used {used} blocks")
print(f"There is left {blocks} blocks")
